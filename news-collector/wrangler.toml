name = "news-collector"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Workers AI 绑定
[ai]
binding = "AI"

# 环境变量（部署时通过 wrangler secret 设置）
# SHARED_SECRET — Pixelverse 调用 Worker API 时的认证密钥
